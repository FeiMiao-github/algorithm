#include <comm.h>

class Solution {
public:
    bool search(vector<vector<int>>& board, string word, std::pair<int, int> baxis, int widx)
    {
        
    }

    bool exist(vector<vector<char>> &board, string word) {

    }
};

void test(vector<vector<char>> &board, string word) {}

int main() { return 0; };
